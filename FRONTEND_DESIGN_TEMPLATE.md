# Deeptop 前端重置设计模板

## 1. 设计读取

这是一个面向长时间使用的桌面 AI 工作台，不是营销页，也不是数据驾驶舱。主要用户需要在会话、代码、工具结果、任务和运行状态之间快速切换，因此界面优先保证阅读舒适、信息分组清楚和操作反馈明确。

设计语言：编辑器工作台（editorial workbench）。

设计参数：

- `DESIGN_VARIANCE: 4`：允许工作区、对话区、辅助面板有清晰的横向分区，但不使用跳跃式构图。
- `MOTION_INTENSITY: 3`：只保留状态变化、展开收起、拖拽和焦点反馈；不使用持续渐变、漂浮或装饰性动画。
- `VISUAL_DENSITY: 5`：工作区保持可扫描的密度，正文和代码区域留出足够行距。

## 2. 视觉原则

### 2.1 先读后操作

对话正文是页面主角。消息内容使用 15px 起步、1.75 行距和约 760px 的阅读宽度。时间、来源、路径等元信息退到辅助层，不与正文争夺视觉重量。

### 2.2 分区代替堆卡片

主界面使用三段结构：顶部窗口栏、左侧会话导航、右侧会话工作区。会话区内部使用边界线、留白和标题层级组织内容；只有工具调用、审批、任务清单和模态对话等真正需要独立层级的内容才使用面板。

### 2.3 颜色只承担语义

整页只使用一个品牌强调色：砖红 `#b65342`。它用于主操作、当前选中、焦点和需要用户注意的状态。绿色只用于成功状态，琥珀色只用于等待或警告，不能把状态色当作装饰色。

### 2.4 明确的形状规则

- 页面和工作区：不使用圆角。
- 普通面板、输入区、弹层：统一 `10px` 圆角。
- 小按钮、标签、输入控件：统一 `7px` 圆角。
- 状态点和复选标记：使用圆形或方形，不混用胶囊容器。

### 2.5 文字尺度

| 用途 | 尺寸 | 行距 | 备注 |
| --- | ---: | ---: | --- |
| 页面标题 | 21-24px | 1.25 | 只用于会话标题、设置标题 |
| 区域标题 | 16-18px | 1.35 | 任务、运行台、设置分组 |
| 正文 | 15px | 1.75 | 对话、说明、表单帮助文字 |
| 控件文字 | 13px | 1.4 | 按钮、下拉、列表操作 |
| 辅助文字 | 12px | 1.45 | 时间、路径、状态说明 |
| 代码 | 13px | 1.65 | 命令、JSON、工具输入输出 |
| 仅元数据 | 11px | 1.4 | 只用于不可避免的技术标记 |

禁止将 10px 或更小字号用于可读内容。禁止用字距、全大写或连续点号制造“设计感”。

## 3. 色彩令牌

### 浅色主题

```text
窗口栏       #202523
侧栏         #29312e
侧栏文字     #e5ebe6
页面背景     #f7f7f4
抬升表面     #ffffff
次级表面     #eff1ed
主文字       #25302c
次级文字     #59655f
弱文字       #76827b
边界         #d8dfd9
强边界       #bcc8c0
强调色       #b65342
强调深色     #944536
成功         #4f7b61
等待         #a47735
错误         #b34f43
```

### 深色主题

深色主题保持相同的层级关系，不切换为霓虹风：

```text
窗口栏       #202427
侧栏         #2b3033
页面背景     #252a2e
抬升表面     #2d3338
次级表面     #353c41
主文字       #edf0f1
次级文字     #c0c7ca
弱文字       #98a2a7
边界         #454e54
强调色       #d47a61
强调深色     #ef9b7f
成功         #87b894
等待         #d7a763
错误         #ed8c7d
```

不使用纯黑、纯白、紫色渐变、外发光或随机透明玻璃层。背景图功能仍保留，但必须经过统一遮罩，不能破坏正文对比度。

## 4. 页面骨架

```text
┌─────────────────────────────────────────────────────────────┐
│ DSH Desktop    项目  编辑       当前工作区       状态  设置  窗口 │  44px
├───────────────┬─────────────────────────────────────────────┤
│ 新会话         │ 会话标题                      任务 轨迹 导出 ... │  64px
│ 搜索           ├─────────────────────────────────────────────┤
│ 会话           │                                               │
│                │              会话内容 / 空状态 / 轨迹           │
│ 会话列表       │                                               │
│                ├─────────────────────────────────────────────┤
│ 工作目录       │ 输入消息...                  模型       发送      │
└───────────────┴─────────────────────────────────────────────┘
  320px          minmax(0, 1fr)
```

- 顶部窗口栏固定 44px，内容单行显示。
- 左栏默认 320px，可拖动到 300-440px；小于 760px 时隐藏，保留工作区和移动端弹层。
- 会话标题区 64px，标题、当前 Preset、工作目录和操作按钮有明确分层。
- 对话区使用独立滚动，不让页面整体跟随输入区跳动。
- 输入区使用稳定高度的多行输入框，模型选择、模式选择和发送动作位于同一控制行。

## 5. 组件模板

### 窗口栏

- 品牌名称使用 14px 粗体，不再使用过小的装饰字标。
- 菜单、工作区和运行状态均为可读文字。
- 设置按钮与窗口控制按钮有 32px 命中区域，关闭按钮只在悬停时使用错误色。

### 会话侧栏

- “新会话”是唯一高权重按钮，采用强调色实底，文字保持一行。
- 搜索框高度 38px，标签和输入文字至少 13px。
- 工作区分组使用 12px 辅助标题，展开后的会话行以 58px 为基线；标题允许最多两行，时间和路径保持独立辅助行，组间只保留 8px 节奏。
- 当前会话使用左侧 3px 强调线和浅色背景，不使用整块高饱和填充。
- 空列表必须说明当前状态；右键菜单和工作目录菜单使用同一弹层规则。

### 会话标题区

- 标题 17px，工作目录和 Preset 12px。
- 任务、轨迹、导出、ZIP、分叉均为 32px 高的次级操作；状态数量使用真实语义，不用装饰圆点。
- 超窄窗口允许隐藏路径和低频操作，但不能让标题换成两行。

### 消息流

- 用户、助手、系统消息保留原始投影结构，不重写数据模型。
- 用户和助手使用左侧角色栏 + 正文的结构，正文不放在高对比气泡里。
- 工具调用使用可展开的分栏面板，调用与结果并列；错误使用错误色边界和明确文字。
- Think、Workflow、注入上下文、生成文件等仍作为同级时间线条目，默认摘要可扫描，展开后读取详情。
- Markdown、表格、代码和图片共享正文尺度，代码区域使用冷灰表面，不使用荧光主题。
- “正在工作”只使用短暂的状态脉冲和普通文字，不使用彩色渐变文字。

### 空状态

- 空状态说明当前动作，不做营销文案。
- 标记、标题、说明、工作目录/模型信息和 Agent Preset 选择按垂直层级排列。
- 选择 Preset 的下拉菜单是独立弹层，选中状态使用左边框和对比背景。

### 输入区

- 输入框正文 15px，最小高度 96px，聚焦时只出现 2px 外轮廓。
- 图片附件显示缩略图、文件名和移除动作。
- 排队/插入为分段按钮，当前模式用浅强调色背景。
- 模型选择保持可读宽度，模型与思考程度在同一菜单内分层。
- 主按钮使用强调色实底，停止动作使用错误色文字和边框，二者文字对比度必须通过 WCAG AA。
- 候选列表锚定输入区上方，不遮挡输入控件。

### 任务与子 Agent

- 任务面板和子 Agent 抽屉是工作层，不是装饰卡片；使用 10px 圆角和统一阴影。
- 任务状态依次使用完成、进行中、待处理三种语义色，进度条只表达真实完成度。
- 子 Agent 书签只在确有子 Agent 时出现，打开后以抽屉展示消息、工具和追问输入。

### 运行台与设置

- 统一使用右侧 680px 抽屉，背景、标题、标签页、内容面板共享同一套令牌。
- 运行台先显示运行状态、Profile、目录和会话统计，再显示命令目录。
- 设置使用左侧导航 + 右侧内容，导航宽 176px；每个设置项采用“标题、说明、控件”两列结构。
- 状态为空、加载中、错误时使用同一内容宽度和明确的说明文字。

## 6. 交互与状态

- 任何按钮必须有 `:hover`、`:active`、`:focus-visible`、`:disabled` 视觉状态。
- 加载状态使用骨架或文字状态，不使用无上下文的全屏 spinner。
- 异常就地显示在触发它的内容区域，保留可操作的重试或关闭动作。
- 弹层支持点击背景关闭和 `Escape` 关闭；焦点环使用强调色，不能被 `outline: none` 吞掉。
- 会话重命名只从会话右键菜单进入，使用应用内对话框，不使用浏览器 `prompt`；标题区不显示重命名入口。
- 动画只表达层级进入、展开收起、状态变化和按钮反馈。尊重 `prefers-reduced-motion`。

## 7. 响应式规则

- `>= 1120px`：左栏、主会话、任务面板按网格共存。
- `760px - 1119px`：任务面板保持悬浮，主会话收窄，低频标题操作减少。
- `< 760px`：隐藏会话左栏，顶部保留工作区与核心操作；设置、任务、子 Agent 改为全宽弹层/抽屉。
- `< 500px`：输入区控制行允许换行，模型选择宽度限制为 150px，所有正文左右边距不小于 12px。
- 不使用 `h-screen` 锁死移动端高度，主壳使用 `100dvh` / `100svh`。

## 8. 实现边界

- 不引入新的 UI 依赖、图标依赖或动画依赖。
- 保留现有 React 组件、事件回调、Tauri bridge、会话投影、工具结果配对和设置数据。
- 这次重置只改变视觉令牌、排版、布局和组件状态样式；不静默修改 URL、持久化字段、菜单含义或桥接协议。
- CSS 变量是唯一的颜色和字号来源；组件不直接写新的颜色体系。
- 视觉重置版本首次加载时只迁移主题和侧栏宽度到新基线，不删除会话、Host、模型或其他业务数据；之后仍可在设置中切换主题和调整宽度。

## 9. 交付前检查

- [ ] 默认浅色和手动深色主题层级一致。
- [ ] 正文最小 15px，辅助文字最小 11px，所有按钮文字可读且不换行。
- [ ] 运行台、设置、任务、子 Agent、工具结果和弹层在 1280px 与 390px 宽度下不互相遮挡。
- [ ] 空状态、加载、错误、禁用、聚焦、展开和选中状态均有视觉反馈。
- [ ] `prefers-reduced-motion` 下无持续动画。
- [ ] `npm run build` 与 `npm run test:bridge` 通过。
- [ ] `git diff --check` 通过，且不修改与本次视觉重置无关的桥接行为。
